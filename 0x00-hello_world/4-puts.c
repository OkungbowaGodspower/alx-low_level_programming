#include <stdio.h>

/**
  * main - starting (entry) point of the program
  * message - it's a variable that stores the value
  * @puts - write the string to standard output
  *
  * Return: returns (0) for success
  */

int main(void)
{
	char message = "Programming is like building a multilingual puzzle";

	puts(message);

	return (0);
}
